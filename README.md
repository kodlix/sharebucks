# ShareBucks

ShareBucks is a Vite + TanStack Start application for tracking and settling shared group expenses.

## Getting started

Install dependencies and run the app locally.

```sh
npm install
npm run dev
```
